import 'package:flutter/material.dart';

class OstrumWidget extends StatelessWidget {
  const OstrumWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ostrum Widget'),
      ),
    );
  }
}
